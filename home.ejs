<%- include('partials/header'); %>
    <h1>home</h1>

    <p>
        <%= homeInfo %>
    </p>
    
    
        <h2>Posts</h2>
       
       <% posts.forEach(function(post) { %>
        <h3> <%= post.title; %></h3>
        <!--注意，href里面的动态值必须也扩在‘’里面-->
       <p> <%= post.body.substring(0,100) + '...'; %> <a href="/post/<%=post.title%>" > Read More</a> </p>
        
        <% }); %>
        
        
        
    
    <%- include('partials/footer'); %>